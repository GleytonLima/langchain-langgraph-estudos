meta {
  name: Resume
  type: http
  seq: 4
}

post {
  url: {{suporteUrl}}/resume
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "thread_id": "{{threadId}}",
    "decisions": [
      {
        "tool_call_id": "{{toolCallId}}",
        "action": "approve"
      }
    ]
  }
}
